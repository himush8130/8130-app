# Supabase project config
# Used by `supabase` CLI when running locally or linking to the cloud project.
# The CLI itself is not yet installed — this file is a placeholder so
# the structure is in place. Full config will be generated by
# `supabase init` once the CLI is installed (deferred to db-engineer).

project_id = "8130-app-dev"
