// =====================================================================
// 8130 APP — Latest release note
// =====================================================================
// Bumped manually with each meaningful change. Displayed in the footer
// of the manager home page so the team knows what's new since they last
// opened the app. Build time below is injected automatically by Vite.
// =====================================================================

export const LATEST_NOTE = 'אינדיקציית "i" כתומה בקריאה עם הערות. כפתור לוג ההערות נצבע בכתום עם מספר ההערות הפתוחות של מנהלים אחרים. מסירה חלקית של כמות פריט במחסן — שורה מתפצלת לכמות שנמסרה + כמות שנותרה ממתינה. בטבלת מק״טים חסומים: שורות עם מק״ט חליפי לבנות עם כפתור הסתרה, הספירה בכותרת רק לחסומים ללא חליפי.'

export const BUILD_TIME = __BUILD_TIME__  // ISO string injected at build
